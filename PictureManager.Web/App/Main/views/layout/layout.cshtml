<div class="navbar navbar-inverse navbar-fixed-top" role="navigation" ng-controller="app.controllers.views.layout as vm">
    <div class="container">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="#/">PictureManager!</a>
        </div>
        <div class="collapse navbar-collapse">
            <ul class="nav navbar-nav">
                <li ng-class="{active: activeMenu == route.config.menuItem}" ng-repeat="route in vm.routes" data-ng-hide="!isAuth">
                    <a ng-href="#{{route.url}}" ng-show="route.config.menuVisible">{{route.config.menuText}}</a>
                </li>
            </ul>
            <ul class="nav navbar-nav navbar-right" data-ng-hide="!isAuth">
                <li><a href="#/" data-ng-click="goToProfile(userName)">Привет, {{userName}}!</a></li>
            </ul>
            <ul class="nav navbar-nav navbar-right" data-ng-hide="isAuth">
                <li><a href="#/singup">Регистрация</a></li>
            </ul>
            <ul class="nav navbar-nav navbar-right" data-ng-hide="isAuth">
                <li><a href="#/login">Войти</a></li>
            </ul>
            <ul class="nav navbar-nav navbar-right" data-ng-hide="!isAuth">
                <li><a href="" data-ng-click="logOut()">Выйти</a></li>
            </ul>
        </div>
    </div>
</div>

<div class="container">
    <div data-ng-view class="shuffle-animation"></div>
</div>

